## Inversion research

This is a tool that collects traces(Linux-based only) and makes inversion research:

#### Usage:

To collect trace, you should do in your terminal app:

```bash
sudo src/collect_trace.sh
```

To gain analysis results, you should **TODO**:
```bash
time python3 src/analysis_pipeline.py --parsing --analysis --input ${path_to_your_brilliant_trace}
```
